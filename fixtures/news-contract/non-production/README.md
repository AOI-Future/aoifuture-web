# Non-production contract fixtures

These files are contract-test inputs only. They are not publication manifests and must not be copied into a production content directory. The public text was manually reduced from the readable 2026-07-23 DailyNews roundup; that roundup is editorial input, not automatic publication authority. Source-read receipts are private fixture evidence and are never emitted by the importer.

The `previous_contexts` entry is the immediately preceding state. The candidate in `contexts` appends exactly one evidence-backed revision and contains two revisions in total. `import-bundle.json` is the complete validator/import input.
